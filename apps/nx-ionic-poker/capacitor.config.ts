import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'nx-ionic-poker',
  webDir: '../../dist/apps/nx-ionic-poker',
  bundledWebRuntime: false,
};

export default config;
